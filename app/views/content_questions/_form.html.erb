<%= form_for(@content_question) do |f| %>
  <% if @content_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_question.errors.count, "error") %> prohibited this content_question from being saved:</h2>

      <ul>
      <% @content_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :tree_id %><br>
    <%= f.text_field :tree_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
