<%= simple_form_for([@course,@tree]) do |f| %>
  <% if @tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tree.errors.count, "error") %> prohibited this tree from being saved:</h2>

      <ul>
      <% @tree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

</br>
<h2>Usuario conectado: <%= @username %></h2> 




      <!--div class="input-group">
        <span class="input-group-addon" id="basic-addon1">@</span>
        <input type="text" class="form-control" placeholder="Username" aria-describedby="basic-addon1">
      </div-->
<div class="input-group">
  <div class="input-group-addon">Contenido</div>
  <div class="form-control"><%= @tree.content.text %></div>
</div>


<br>
   

<table style="width:100%">
    <tr>
      <td style="width:50%">

        <h5>Pregunta inicial de contenido (1.1)</h5>
         <%= f.fields_for :initial_content_question do |question_fields| %>
            <%= question_fields.text_area :question, :readonly => true, size: "60x5",:class=>"form-control" %>

          <br>
            <%= @tree.initial_content_question.content_choices.each do |choice| %>

             <%= f.fields_for :content_choices do |choice_fields| %>
            <div class="input-group">
              <span class="input-group-addon" id="check">
                <%= choice_fields.check_box :right, :class => 'inline'%>
              </span>
              <%= choice_fields.label :text, :readonly => true, :class => "form-control" %>
            </div>
            <% end %>
            <br>
          <% end %>

         <% end %>
        <br>
      </td>


      <td>
        <h5>Pregunta inicial de pensamiento crítico (1.1)</h5>
         <%= f.fields_for :initial_ct_question do |question_fields| %>
            <%= question_fields.text_area :question, :readonly => true, size: "60x5",:class=>"form-control" %>

          <br>
            <%= @tree.initial_ct_question.ct_choices.each do |choice| %>

             <%= f.fields_for :ct_choices do |choice_fields| %>
            <div class="input-group">
              <span class="input-group-addon" id="check">
                <%= choice_fields.check_box :right, :class => 'inline'%>
              </span>
              <%= choice_fields.label :text, :readonly => true, :class => "form-control" %>
            </div>
            <% end %>
            <br>
          <% end %>
          hola
         <% end %>
        <br>
      </td>
       
    </tr>
</table>

<br>
 <button type="button" class="btn btn-default">Contiuar</button>

<% end %>