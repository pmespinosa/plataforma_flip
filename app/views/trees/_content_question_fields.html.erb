<div class='nested-fields'>
	<div class="input-group">
		 <%= f.text_area :question, size: "60x5",:class=>"form-control", :required=> true %>
	</div>
	<br>
	<%= f.fields_for :content_choices do |builder| %>
	  <%= render 'content_choice_fields', :f => builder %>
	<% end %>
	<div class='nested-fields'>
		<%= link_to_add_association '<span class=" glyphicon glyphicon-plus-sign"></span>'.html_safe, f, :content_choices, :class => "btn btn-default" %>
	</div>
</div>
