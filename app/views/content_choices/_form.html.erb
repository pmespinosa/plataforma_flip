<%= form_for(@content_choice) do |f| %>
  <% if @content_choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_choice.errors.count, "error") %> prohibited this content_choice from being saved:</h2>

      <ul>
      <% @content_choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :right %><br>
    <%= f.check_box :right %>
  </div>
  <div class="field">
    <%= f.label :content_question_id %><br>
    <%= f.text_field :content_question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
